# 🚀 AEX-Agent Execution Guide\n\n## Status: READY TO RUN ✅\n\n---\n\n## **STEG 1: Miljö Setup (2 minuter)**\n\n### Linux/macOS:\n```bash\ncd mitt-projekt\npython -m venv venv\nsource venv/bin/activate\npip install -r requirements.txt\nmkdir -p logs data cache\n```\n\n### Windows (PowerShell):\n```powershell\ncd mitt-projekt\npython -m venv venv\n.\\venv\\Scripts\\Activate\npip install -r requirements.txt\nmkdir logs, data, cache\n```\n\n✅ **Verifica**: Du ska se `(venv)` i din terminal\n\n---\n\n## **STEG 2: Starta Agenten (1 minut)**\n\n```bash\npython agent/core/main.py\n```\n\n**Förväntat output:**\n```\n2026-07-26 12:00:00 - __main__ - INFO - Initializing AEX-Agent v1.0.0\n2026-07-26 12:00:00 - __main__ - INFO - Agent is now running\n2026-07-26 12:00:00 - __main__ - INFO - Agent initialized successfully\n```\n\n✅ **Agenten kör!**\n\n---\n\n## **STEG 3: Testa Agentens Mönster (3 minuter)**\n\n### Terminal 2 (ny tab):\n```bash\ncd mitt-projekt\nsource venv/bin/activate  # Windows: .\\venv\\Scripts\\Activate\npython -m pytest tests/test_patterns.py -v\n```\n\n**Förväntat output:**\n```\ntests/test_patterns.py::TestPattern1::test_role_definition PASSED\ntests/test_patterns.py::TestPattern1::test_constraints_set PASSED\ntests/test_patterns.py::TestPattern2::test_verification_chain PASSED\n...\n======================== 12 passed in 0.45s ========================\n```\n\n✅ **Alla tester passerar!**\n\n---\n\n## **STEG 4: Starta Web API (2 minuter)**\n\n### Terminal 3 (ny tab):\n```bash\ncd mitt-projekt\nsource venv/bin/activate  # Windows: .\\venv\\Scripts\\Activate\npython platforms/web/flask_app.py\n```\n\n**Förväntat output:**\n```\n* Running on http://0.0.0.0:5000\n* WARNING: This is a development server\n```\n\n✅ **API kör på http://localhost:5000**\n\n---\n\n## **STEG 5: Testa API Endpoints (2 minuter)**\n\n### Terminal 4 (ny tab) eller Web Browser:\n\n#### Health Check:\n```bash\ncurl http://localhost:5000/health\n```\n\n**Förväntat:**\n```json\n{\n  \"success\": true,\n  \"timestamp\": \"2026-07-26T12:00:00.000000\",\n  \"data\": {\n    \"status\": \"online\",\n    \"version\": \"1.0.0\"\n  },\n  \"error\": null\n}\n```\n\n#### Agent Status:\n```bash\ncurl http://localhost:5000/api/agent/status\n```\n\n**Förväntat:**\n```json\n{\n  \"success\": true,\n  \"data\": {\n    \"agent_status\": \"initialized\",\n    \"active_tasks\": 0\n  }\n}\n```\n\n#### Skapa Task:\n```bash\ncurl -X POST http://localhost:5000/api/tasks \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"task_type\": \"data_processing\"}'\n```\n\n**Förväntat:**\n```json\n{\n  \"success\": true,\n  \"data\": {\n    \"task_id\": \"task_1\"\n  }\n}\n```\n\n#### Hämta Task Status:\n```bash\ncurl http://localhost:5000/api/tasks/task_1\n```\n\n✅ **API funkar perfekt!**\n\n---\n\n## **STEG 6: Verifiera Guard Rails (2 minuter)**\n\n### Terminal 4:\n```bash\npython -c \"\nfrom agent.patterns.pattern7_guard_rails import GuardRailSystem\nsystem = GuardRailSystem()\nsystem.print_guard_rails()\n\n# Test spending limit\nprint('\\nTesting Spending Guard Rail:')\nprint(f'  $0.30 cost allowed: {system.check_spending(0.30)}')\nprint(f'  $1.00 cost allowed: {system.check_spending(1.00)}')\n\n# Test domain access\nprint('\\nTesting Communication Guard Rail:')\nprint(f'  api.github.com allowed: {system.check_communication(\"api.github.com\")}')\nprint(f'  evil.com allowed: {system.check_communication(\"evil.com\")}')\n\"\n```\n\n✅ **Guard Rails aktiva!**\n\n---\n\n## **STEG 7: Error Recovery Test (2 minuter)**\n\n```bash\npython -c \"\nfrom agent.patterns.pattern5_error_recovery import ErrorRecoveryHandler, ErrorCode\n\nhandler = ErrorRecoveryHandler()\n\nprint('Testing Error Recovery Strategies:')\n\n# Test recoverable error\nresult = handler.handle_error(\n    ErrorCode.RATE_LIMIT,\n    'API rate limited',\n    {'source': 'external_api'}\n)\nprint(f'\\nRate Limit (recoverable):')\nprint(f'  Action: {result[\\\"action\\\"]}')\nprint(f'  Max Retries: {result[\\\"max_retries\\\"]}')\nprint(f'  Wait: {result[\\\"wait_seconds\\\"]}s')\n\n# Test unrecoverable error\nresult = handler.handle_error(\n    ErrorCode.MISSING_CREDENTIALS,\n    'API key missing',\n    {'env_var': 'API_KEY'}\n)\nprint(f'\\nMissing Credentials (unrecoverable):')\nprint(f'  Action: {result[\\\"action\\\"]}')\nprint(f'  Message: {result[\\\"message\\\"]}')\n\"\n```\n\n✅ **Error Recovery funkar!**\n\n---\n\n## **STEG 8: Debug & Logging (Ongoing)**\n\n### Se loggar i real-time:\n```bash\ntail -f logs/agent.log\n```\n\n### Se systemstatus:\n```bash\nls -la logs/\n```\n\n### Rensa loggar:\n```bash\nrm logs/*.log\n```\n\n---\n\n## **STEG 9: Kontrollista - Allt Funkar? ✅**\n\n- [x] Python venv aktiverad\n- [x] Dependencies installerade\n- [x] Agent startar utan fel\n- [x] Alla Pattern tests passerar\n- [x] Web API svarar på requests\n- [x] Guard Rails är aktiva\n- [x] Error Recovery funkar\n- [x] Loggning fungerar\n\n---\n\n## **🎯 Nästa: Production Setup**\n\n### För iOS:\n```bash\ncd platforms/ios\n# Öppna i Xcode och kör\n```\n\n### För Android:\n```bash\ncd platforms/android\n# Öppna i Android Studio och kör\n```\n\n### För Linux Service:\n```bash\nsudo cp platforms/linux/aex-agent.service /etc/systemd/system/\nsudo systemctl daemon-reload\nsudo systemctl enable aex-agent\nsudo systemctl start aex-agent\nsudo systemctl status aex-agent\n```\n\n### För Windows Service:\n```powershell\ncd platforms/windows\npython install_service.py\nsc start AEXAgent\nsc query AEXAgent\n```\n\n---\n\n## **🚨 Troubleshooting**\n\n### Problem: \"command not found: python\"\n**Lösning:** Använd `python3` istället\n```bash\npython3 -m venv venv\npython3 -m pip install -r requirements.txt\n```\n\n### Problem: \"ModuleNotFoundError\"\n**Lösning:** Uppdatera PYTHONPATH\n```bash\nexport PYTHONPATH=$PYTHONPATH:$(pwd)\n```\n\n### Problem: \"Port 5000 already in use\"\n**Lösning:** Använd annan port\n```bash\npython platforms/web/flask_app.py --port 5001\n```\n\n### Problem: \"Permission denied\" på Linux\n**Lösning:** Ge execute-permission\n```bash\nchmod +x agent/core/main.py\n```\n\n### Problem: Venv inte aktiverad\n**Lösning:** Aktivera igen\n```bash\nsource venv/bin/activate  # Linux/macOS\n.\\venv\\Scripts\\Activate   # Windows PowerShell\n```\n\n---\n\n## **📊 Performance Monitoring**\n\n```bash\n# Se CPU/Memory usage\nwatch -n 1 'ps aux | grep python'\n\n# Se network connections\nnetstat -an | grep 5000\n\n# Se open files\nlsof -i :5000\n```\n\n---\n\n## **✨ Du är nu redo!**\n\n**Agenten kör på:**\n- Core: `http://localhost` (Process)\n- API: `http://localhost:5000`\n- Logs: `./logs/agent.log`\n\n**Total setup time: ~15 minuter**\n\n🎉 **Lycka till!**\n"